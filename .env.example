# Example .env file for Syntax Highlighter API

# Set your secret API key for authenticating requests - use it to access the API
API_KEY=your_api_key_here

# Port to run the server on (default: 3000)
PORT=4000
