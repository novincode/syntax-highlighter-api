# Example .env file for Syntax Highlighter API

# Set your secret API key for authenticating requests
API_KEY=your_api_key_here

# Port to run the server on (default: 3000)
PORT=3000

# Enable request logging (true/false)
LOGGER=false
